function HackerChall(s) {
	/* 
Must add up to 9 in all directions, no duplicates



*/

	let max;

	// Calculate total

	max = s.length * s.length;

	// 1. find if any duplicates

	for (let i = 0; i < s.length; i++) {} // original for

	// 2. calculate in every direction
}

let mat = [
	[4, 9, 2],
	[3, 5, 7],
	[8, 1, 5],
];
console.log(HackerChall(mat));
